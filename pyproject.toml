[project]
name = "timepredict-agent"
version = "0.1.0"
description = "A local agent for collecting and summarizing recent time-series forecasting papers."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
pdf = ["pymupdf>=1.24"]
mysql = ["pymysql>=1.1"]

[project.scripts]
timepredict-agent = "timepredict_agent.cli:main"
